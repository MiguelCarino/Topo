{
  "name": "nettopo-tests",
  "private": true,
  "type": "module",
  "description": "Cross-engine test runner (Playwright) for the Topo editor.",
  "scripts": { "cross": "node run-cross.mjs" },
  "devDependencies": { "playwright": "^1.40.0" }
}
